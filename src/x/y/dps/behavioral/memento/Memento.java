package x.y.dps.behavioral.memento;

/**
 * 备忘录主要用来存储原发起对象的内部状态，但是具体需要存储哪些数据是由原发器对象来决定的。
 * 另外备忘录对象应该只能由原发器对象来访问它内部的数据，原发器外部的对象不应该访问到备忘录对象的内部数据
 * 备忘录的窄接口，没有任何方法定义
 */
public interface Memento {
    //
}
